@model IEnumerable<UniPlanner.Models.MajorsOffered>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Link)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UniProgramme)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Link)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UniProgramme.UniProgrammeID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MajorsOfferedID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MajorsOfferedID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MajorsOfferedID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
